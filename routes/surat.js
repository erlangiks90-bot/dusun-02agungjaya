const express=require('express');const router=express.Router();const QRCode=require('qrcode');const supabase=require('../services/supabase');const{requireLogin}=require('../middleware/auth');let demoSurat=[];
router.get('/',requireLogin,async(req,res)=>{try{let q=supabase.from('surat_pengantar').select('*').order('created_at',{ascending:false});if(req.session.user.role==='rt')q=q.eq('rt',req.session.user.rt);if(req.session.user.role==='masyarakat')q=q.eq('nik',req.session.user.nik);const{data,error}=await q;if(error)throw error;res.json(data||[])}catch{res.json(demoSurat)}});
router.post('/',requireLogin,async(req,res)=>{const payload={nomor_surat:req.body.nomor_surat||'',nik:req.body.nik||req.session.user.nik||'',nama:req.body.nama,rt:req.body.rt||req.session.user.rt||'',keperluan:req.body.keperluan,status:req.session.user.role==='masyarakat'?'Menunggu RT':'Menunggu Kadus'};try{const{data,error}=await supabase.from('surat_pengantar').insert(payload).select().single();if(error)throw error;res.json(data)}catch{const row={id:Date.now(),...payload,created_at:new Date().toISOString()};demoSurat.unshift(row);res.json(row)}});
router.put('/:id/status',requireLogin,async(req,res)=>{if(!['kadus','rt'].includes(req.session.user.role))return res.status(403).json({error:'Tidak diizinkan'});const status=req.body.status;try{const{data,error}=await supabase.from('surat_pengantar').update({status}).eq('id',req.params.id).select().single();if(error)throw error;res.json(data)}catch{const row=demoSurat.find(s=>String(s.id)===String(req.params.id));if(row)row.status=status;res.json(row||{ok:true})}});
router.get('/:id/qrcode',async(req,res)=>{const url=`${req.protocol}://${req.get('host')}/verifikasi.html?id=${req.params.id}`;const qr=await QRCode.toDataURL(url);res.json({qr,url})});module.exports=router;
