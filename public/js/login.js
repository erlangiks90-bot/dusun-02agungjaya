async function api(url,options={}){const r=await fetch(url,{headers:{'Content-Type':'application/json'},...options});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'Error');return d}
async function login(){msg.textContent='Memeriksa...';try{const data=await api('/api/auth/login',{method:'POST',body:JSON.stringify({role:role.value,username:username.value.trim(),password:password.value})});if(data.user.role==='kadus')location.href='/dashboard-kadus';else if(data.user.role==='rt')location.href='/dashboard-rt';else location.href='/dashboard-masyarakat'}catch(e){msg.textContent=e.message}}
